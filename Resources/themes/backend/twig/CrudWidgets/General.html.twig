        <ul class="nav nav-list">
            <li class="nav-header">General information</li>
            <li>
            {% if view.length == 0 %}
                <p>There are no items.</p>
            {% elseif view.length == 1 %}
                <p>There is {{ view.length }} item.</p>
            {% else %}
                <p>There are {{ view.length }} items.</p>
            {% endif %}
            </li>

            {% if is_granted(crud.entity.roles.create) %}
            <li>
                <p>
                    <a class="btn" href="{{ ref(['_table/',crud.entity.name,'/0']) }}">Add a new item</a>
                </p>
            </li>
            {% endif %}
        </ul>
